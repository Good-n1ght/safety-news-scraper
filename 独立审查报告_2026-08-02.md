# 强安系列_手机展示 — 独立代码审查报告

> **审查日期**：2026-08-02
> **审查方式**：独立审查（未采信本项目既有的 AI 审计文档结论，由 4 个审查单元并行独立实测代码得出）
> **审查范围**：整个 `强安系列_手机展示/` 目录（主 HTML ×2、scripts、extensions、proxy、.github/workflows、手机演示包、git 历史、既有文档）
> **性质**：本次为只读审查，未修改任何文件。所有结论均附代码位置与证据，供复验。

---

## 〇、总体评价

项目整体可用，抓取管道（CORS 代理实测可用、cron 时区正确、权限最小化、超时覆盖完整）和素材管线设计有相当成熟度。但存在 **4 个高危缺陷（含 1 个已公开泄露的 API Key）**、**12+ 个中危缺陷**，且**既有 AI 文档（尤其 CODE_AUDIT.md）与代码实际状态多处矛盾**，若干声称"已修复"的问题实际未修或未同步。

**给维护者的第一句话**：请先吊销博查 API Key（已在公开仓库泄露），再谈其他。

---

## 一、紧急问题（高危，建议立即处理）

### 1.1 博查 API Key 明文硬编码，且已随公开仓库泄露 ⚠️最高优先

- **位置**：`强安兴企安全园地生文助手.html:1724、1757`（`searchBochaAPI` 请求头，活跃代码路径，非死代码）；`手机演示包/强安兴企安全园地生文助手.html:1724、1757`；`生文助手.html:1720、1753` 三处同步存在
- **证据**：`"Authorization": "Bearer sk-a980fe7a23034a7893367238ac828159"`；`git remote` 为 `git@github.com:Good-n1ght/safety-news-scraper.git`，项目已部署 GitHub Pages（公开仓库）
- **后果**：任何拿到页面/仓库（含 git 历史）的人可盗用该 Key 消耗博查免费额度（1000 次/月）或滥用。Key 已不可撤回，**必须吊销并重新签发**
- **补充**：KNOWLEDGE.md 坑 15 记录过更早版本曾泄露另一把 Key（`sk-64c2daf…`），历史泄露在 git 历史中永久可查
- **修复方向**：Key 移出源码——从设置面板 / localStorage 读取（`DEFAULT_API_KEY` 已存在但默认为空），或经后端/代理注入

### 1.2 「保存到历史」功能完全不可用（gistTokenInput 未注册）

- **位置**：`强安兴企安全园地生文助手.html:1355-1366`（`initSettings` 的 els 映射）vs `:1491`（保存处理）
- **证据**：els 对象只有 `gistIdInput`，**没有 `gistTokenInput`**；而保存逻辑写 `var gistToken = els.gistTokenInput ? els.gistTokenInput.value.trim() : ""` —— 恒为 `""`，并执行 `localStorage.removeItem("gist_token")`
- **后果**：每次打开设置点保存都会**清空已填的 GitHub Token**；`saveDraft` 永远 reject「请先在 API 设置里填写 GitHub Token」。核心卖点功能（历史稿件保存）完全不可用
- **修复**：els 中加入 `gistTokenInput: document.getElementById("gistTokenInput")`（一行修复）

### 1.3 强安视界 — AI 写稿正文未转义 → 存储型 XSS

- **位置**：`强安视界_UI改版.html:2406-2426` `renderGeneratedArticles`
- **证据**：`var body = article.article ? article.article.replace(/\n/g, '<br>') : ...` 后直接拼入 innerHTML；同函数中 `<h3>` 标题、source、link 均已转义，**唯独正文没有**
- **攻击链**：新闻标题来自第三方源（头条/百度，发布者可植入 `<img src=x onerror=...>`）→ 标题进 AI 提示词 → 模型常回显到 article JSON → 原样注入 DOM。可窃取 localStorage 中保存的 API Key（`window.DS_API_KEY`、`GIST_TOKEN` 均为全局变量）
- **修复**：正文先 `escapeHtml` 再替换换行

### 1.4 生文助手 — AI 草稿渲染未消毒 → 提示注入链

- **位置**：`强安兴企安全园地生文助手.html:2759` `renderAiDraft` 中 `els.articleBody.innerHTML = cleaned`（全文唯一未转义渲染点）
- **背景**：`draft.content` 来自模型输出，而模型 prompt 中注入了未经信任的联网抓取全文（`buildAISystemPrompt` 拼接搜索正文）——恶意网页可诱导模型输出 `<img src=x onerror=...>` 等
- **修复**：对模型输出做白名单消毒（如 DOMPurify），或要求模型输出 Markdown 后自行渲染

### 1.5 开放代理 / SSRF 风险（部署实例可被全网滥用）

- **位置**：`proxy/index.js:29-48`（`app.js:12-26` 同款）
- **证据**：`app.get("/")` 接受任意 `?url=` 直接服务端 fetch，**无 scheme/域名白名单、无超时（无 AbortSignal）、无响应大小限制**
- **后果**：部署在 Railway/SCF 的实例可被当作匿名转发器（CORS 绕过、恶意中继）；永不响应的目标 URL 可挂死连接
- **修复**：域名白名单 + 仅允许 http/https + `AbortSignal.timeout` + 响应体上限（+ 可选鉴权）

---

## 二、中危缺陷

### 2.1 强安兴企安全园地生文助手.html

| # | 位置 | 缺陷 | 后果 |
|---|------|------|------|
| M1 | :1904-1913 | 「新增」判定：`lastViewAt` 存日期字符串（`toISOString().slice(0,10)`），与 `addedAt` 完整时间戳做字典序比较 | 同一 UTC 日内新条目 `"2026-08-02" < "2026-08-02T08:30:00Z"` 恒真 → badge 全天反复出现；日界用 UTC 而非北京时间，00:00-08:00 北京时段日期错位 |
| M2 | :1867 / :1720 / :2946 | fetchTodayMaterials（Gist 拉取）、searchBocha、loadHistory 均无超时/AbortController（仅 fetchArticleContent 有 12s） | 网络挂起 → 按钮/进度条永久卡死 |
| M3 | :2960-2995 | saveDraft 无去重、无 ETag/If-Match，每次无条件 push | 重复点击/多标签页产生重复稿件且互相覆盖（read-modify-write 竞态）；drafts.json 无限增长 |
| M4 | :1579-1603 | 素材缓存只校验 `cached.materials/savedAt` 存在，不校验元素结构；normalize 在 try 之外 | 缓存损坏（元素 null/缺字段/缺 source）→ init 顶层抛错 → 整页 JS 红屏 |
| M5 | :2197 | `data-material-id="' + item.id + '"` 未转义（同函数其他字段都转义了） | id 来自 Gist/导入数据，被污染时可属性注入 XSS |
| M6 | :1310-1322 / :1462-1495 | 页面加载时 localStorage 顶层直调、保存时 setItem 无 try/catch | 隐私模式/存储禁用 → 页面崩溃或点保存无反应（overlay 不关） |
| M7 | :2001 | html2canvas 未判 typeof，CDN 加载失败时点击「导出日报」抛未捕获 TypeError | 导出按钮静默失灵，无提示 |
| M8 | :1323 / :1496 | `GIST_RAW` 硬编码用户名 `Good-n1ght/`；清空 Gist ID 时 `window.GIST_ID` 不同步 | 用户配置他人 Gist 后 raw 读取 404；清空需刷新生效 |

### 2.2 强安视界_UI改版.html

| # | 位置 | 缺陷 | 后果 |
|---|------|------|------|
| M9 | :2031-2065 + :2539 | 除头条外 16 个 fetch（7 RSS + 4 百度榜 + 5 Bing）全部无超时 | 任一挂起 → `Promise.all` 永不 settle → 内嵌兜底数据永不触发 → 页面永久"正在刷新..."，fetchBtn 永久禁用 |
| M10 | :926 vs :2248 | HTML 有「通俗化表达」复选框（id=groundMode），但全局 els（:1596-1615）无此键，`els.groundMode` 恒 undefined | 该选项永远不生效，用户设置被静默忽略 |
| M11 | :1302-1345 | `fetchPexelsImages`/`injectPexelsImages` 定义后从未被调用（设置面板却有 Pexels API Key 输入框） | 配图功能形同虚设 |
| M12 | :1051-1052 + :1243 | GLM baseUrl `https://open.bigmodel.cn/api/paas/v4` + 硬拼 `/v1/chat/completions` → 实际请求 `/paas/v4/v1/chat/completions`（智谱官方端点应为 `/paas/v4/chat/completions`） | 选中「永久免费」GLM 模型大概率写稿失败（注：网关先鉴权后路由，未认证请求两种路径均 401，未能 100% 证实 404，请对方核实） |
| M13 | :1907-1918 | `hotValue: parseInt(item.hotTag) || 0`，实测百度 s-data 中 hotTag 是**排名字符串**（"0"-"9"），载荷中无 hotScore 字段 | 百度条目热度全是 0-9 名次，40 分热度权重被头条大数值碾压，排序失真 |
| M14 | :2371-2377 | `JSON.parse(raw.match(/\{[\s\S]*\}/)[0])`：模型输出带 ```json 围栏即解析失败，且无重试（注释却声称"含超时重试"） | 写稿结果整篇回退占位文 |
| M15 | :1193-1196 | 顶层 `localStorage.getItem` 无 try-catch | 存储禁用 → 整个内联脚本解析期崩溃，页面完全空白 |
| M16 | :2485-2513 | 两个 click 监听器叠加：点「标题优化/撤销」按钮会命中 `.news-item` 选择逻辑，顺带 `toggleSelect()` | 点按钮意外翻转选中状态并全量重渲染 |

### 2.3 scripts/ 采集管道与基础设施

| # | 位置 | 缺陷 | 后果 |
|---|------|------|------|
| M17 | scrape-safety-news.js :421-454 + :457-487 + :686-691 | Gist B 临时 404（CDN 抖动/误删/转私有）被当作"文件不存在" → 走旧文件迁移 → 双 404 → 返回 [] → 直接以本轮 top-30 覆盖 B | **历史 100 条展示库被重建为 30 条，curated 历史 + addedAt 元数据永久丢失**。现有双保护（只挡"B 有数据但合并空"与"双空"）挡不住"B 实际存在但读取被视为不存在" |
| M18 | :660-679 | B 合并去重按「规范化标题前 20 字符」，**非 URL 去重**（CODE_AUDIT 声称"按 URL 去重"不实） | 模板化标题（"XX市部署煤矿安全生产工作…"）前 20 字符相同的不同文章被误判重复，真实素材被吞 |
| M19 | 全局 | 零重试：Google RSS、Bing、正文抓取、Gist A/B 写入均一次失败即跳过/崩溃；Bing 整层失败静默返回 [] | 静默降级、质量不可观测，管道靠 fallback 兜底后仍显示"成功" |
| M20 | :674-679 | B 历史条目的 compositeScore 入库后从不重算（时间衰减失真） | 前端目前按 score 排序暂未爆雷，但字段语义过期，任何按此排序的消费方会拿到错误顺序 |
| M21 | :426 / :493 | Gist B 读取走无鉴权 raw URL（可能命中 CDN 缓存），写入走 API | 同日 4 次运行可能读到陈旧 B 重复写入 |
| M22 | :653 | Gist A 写入未 try/catch：A 失败整管崩溃、B 完全不更新；B 失败时 A 已覆盖 | 部分成功状态下 A/B 无法对齐 |
| M23 | workflow :39-51 | 失败告警每次失败新建 Issue 无去重（4 次/天最多刷 4 条）；`labels: ["bug"]` 若仓库无此 label，`issues.create` 422，**告警静默丢失** | 告警噪音或完全失效 |
| M24 | scrape-test.js | 与生产脚本 4 处不一致：① 测试缺 timeBonus（+10）；② 测试过滤 `score>=0` 自称"无硬阈值"，生产是 `MIN_SCORE=55`（**直接矛盾**）；③ 测试 B 合并按 compositeScore 重排截断，生产是新素材优先保序；④ 测试 fallback 去重范围含 existing B，生产只看 enriched；且测试缺 B 清空双保护与旧文件迁移 | `--push` 模式会把测试口径（含 55 分以下素材）推上真实 Gist，污染生产展示库 |

### 2.4 proxy/ 与部署配置

| # | 位置 | 缺陷 | 后果 |
|---|------|------|------|
| M25 | package.json:5 / Procfile:1 / railway.json:5 / proxy/package.json:1 / proxy/scf_bootstrap:1 | 同一代理三套入口：Railway/Procfile 跑 `index.js`（完整版），scf_bootstrap 跑 `node app.js`（简化版），proxy/package.json main 是 app.js；根 package.json 依赖 express ^5.2.1、proxy 锁 ^4.18.0（双版本并存） | 部署实例跑哪版不可预期，依赖版本漂移 |
| M26 | README :45/:51 vs 实际 | README 声称"v5 起 CORS 代理全线废弃、getProxy() 废弃"，但根 package.json main/start、Procfile、railway.json 仍部署运行 proxy；config.js 与两个 HTML 仍引用 3 个 SCF 代理 URL，且 `1437883484-k88upcj3r3`（config.js）与 `1437883484-1xfayinfej`、`1437883484-b850njs6yb`（HTML 内硬编码）**互不一致**；getProxy/switchProxy 是无调用者的死代码 | 文档与部署事实严重不符，被废弃的 SCF 函数仍暴露在公网 |

### 2.5 低危（速览，未展开）

- 生文助手：isNew 随缓存持久化（下次打开旧 badge 仍在）；空库导入 `Math.max.apply(null, []) = -Infinity` 伪 id；死代码 5 处（renderDraft/normalizeTrace/normalizeTitle/normalizeTitles/normalizeSections/getProxy/switchProxy）；`els.coverTitle` 指向不存在元素、coverDesc 永远不可见；raw CDN 缓存导致保存后列表不更新；`copyDraft` 仅用 clipboard API 无 execCommand 降级（file:// 下不可用）；NO_RESULTS 分支第二次生成无进度条且错误文案误导；`raw.references` 含 null 时整篇结果被丢弃；导出卡片分类名未转义；`<button>` 内嵌 `<a>` 非法嵌套；注释"15天"与实际 30 天不符
- 强安视界：死代码（parseRSS / fetchPexelsImages / injectPexelsImages / loadRewrittenCache / MODEL_BASE_URLS / hotBadge）；`response_format: json_object` 无模型兼容判断（OpenRouter 部分 :free 模型会 400）；setItem 无 try-catch；渲染全量重建 innerHTML 无文档片段（100+ 条卡顿）；335KB 内嵌 JSON 同步解析阻塞首屏 0.5-1.5s；无 CSP meta；嵌入数据为 2026-07-10 快照但无"数据日期"提示
- 采集管道：`newToB` 统计错误（数的是前 30 个槽位而非实际新增）；hooks.js:96 `Object.keys(Hooks.list)` 恒 []（日志永远显示 0 个钩子点）；正文先抓 6000 字再截 500（92% 浪费）+ 无 charset 处理（GBK 站点乱码）；Google News redirect URL 处理是空操作；fallback_materials.json 中 fb-009/fb-010 链接相同且为首页、fb-001/fb-008 为 http://（https 页面混合内容拦截）
- 仓库卫生：`手机演示包.zip`（241KB 二进制）被 git 跟踪，与目录内容可能漂移（README 声称 14 文件 SHA256 对齐，实际包内仅 10 文件）

---

## 三、文档与实际不符清单（既有 AI 文档的可信度问题）

| # | 文档声称 | 实际情况 |
|---|---------|---------|
| D1 | CODE_AUDIT：`生文助手.html` 已删除，且与主文件 MD5 一致（D2FE7C7A...） | **未删除**，被 git 跟踪，且是**线上入口文件**（git log：`05e0763 fix: 恢复生文助手.html（线上入口文件，误删）`）；MD5 为 `510696df` vs 主文件 `58682a62`，**不一致**；内容为**缺 v5.0.2/v5.0.3 两处修复的旧版**（主文件 8/1 已更新，该文件停在 7/30）→ 线上部署版本落后 |
| D2 | CODE_AUDIT：`proxy/app.js` 已删除 | 仍在磁盘（898B），且被 git 跟踪，是 proxy/package.json 的 main |
| D3 | CODE_AUDIT：手机演示包/汇报页是旧版（footer 2026.07.20）、index.html「略有差异」 | 7/30 已同步：逐对 MD5 完全相同（含 6 份文档），无任何差异；footer 实际为 2026.07.26 |
| D4 | CODE_AUDIT：B "按 URL 去重"；衰减公式 `1.0 - ageDays/14` 最低 0.3 | 实际按**标题前 20 字符**去重；衰减公式为 `1/(1+0.03×days)`，无下限 |
| D5 | CODE_AUDIT：20 关键词"并行"搜索 | 实际串行 + 1.5s 间隔（行为上更安全，但描述不实） |
| D6 | test_phone_diag.html :91 模拟逻辑「lastViewAt 不存在 → 不会标任何新增」 | v5.0.2 起应用对无 lastViewAt 首访采用 **24h 兜底标新增**，诊断页判定逻辑过时，可能误导排查 |
| D7 | README：CORS 代理"全线废弃" | 部署配置仍运行代理；config.js 与 HTML 中 3 个 SCF 代理 URL 互相不一致（见 M26） |
| D8 | CHANGELOG v5.0.2 日期 07-31 / README 头部概述未提 v5.0.2、v5.0.3 | git 提交时间为 08-01；版本表已含但概述行滞后 |
| D9 | CODE_AUDIT 内部矛盾：7/30 审计头「14 文件 SHA256 对齐」vs 正文「2 个旧版/待确认」；「14 文件」计数不准 | 实际 12 个文件全部对齐（7/28 正文未随 7/30 增补同步） |

**根本原因判断**：AI 文档的结论带时间戳，后续改动未回填；审计过程是"描述代码"而非"验证文档"——把 7/28 的旧结论当成了当前事实。这比没有文档更有害（会误导修复方向）。

---

## 四、已实测验证为正常的方面（供对方复验，避免重复劳动）

- **CORS 代理真实可用**：`https://1437883484-b850njs6yb.ap-guangzhou.tencentscf.com/?url=` 实测返回 200 且 `Access-Control-Allow-Origin: *`；rss2json.com 实测 200（注意：此为 HTML 中硬编码的代理 URL，与 config.js 中的 URL 不一致——见 M26）
- **Gist Token 使用正确**：仅经 workflow `env: GIST_TOKEN: ${{ secrets.GIST_TOKEN }}` 注入，脚本从 `process.env` 读取，无硬编码 Gist token
- **workflow 权限最小化**：job 级 `contents: read + issues: write`；cron 时区换算正确（`0 23/05/11/17 * * *` UTC = 北京 07/13/19/次日01:00）
- **依赖无多余**：scripts 仅 cheerio + fast-xml-parser 且均被使用
- **addedAt 补打逻辑正确**：新素材打 nowISO、历史缺失才补、已有保留
- **B 截断策略与设计一致**：新素材优先 + 保序截断 100 条
- **双重清空保护本身有效**（仅 404 重建场景有漏洞，见 M17）
- **超时覆盖完整（管道侧）**：Google/Bing 15s、正文抓取 12s、正文失败降级 summary
- **生文助手 XSS 大部分已封堵**：素材列表/历史列表/tab 等用户可控文本均正确转义；toast 用 textContent；API Key 不进 URL/日志
- **强安视界批量写稿无并发/竞态/越界**：processOne 严格顺序递归、索引边界检查齐全；抓取失败逐源降级不中断
- **强安视界渲染 XSS 大部分已封堵**：新闻卡片 title/source/pubDate/category 均转义，外链 `target="_blank" rel="noopener"`；去重与占比控制正确

---

## 五、建议的修复优先级

**P0 止血（1-2 天）**
1. 吊销并重新签发博查 Key；代码改从设置面板/localStorage 读取（或后端注入）
2. 修复 gistTokenInput 一行 bug（恢复「保存到历史」核心功能）
3. 两处 AI 渲染 XSS（强安视界 :2406、生文助手 :2759）
4. 两个 HTML 统一带超时的 fetch 封装（防永久 loading）
5. 同步 `生文助手.html` 到最新版，或明确弃用并统一线上入口

**P1 可靠性（本周）**
6. Gist 读写改走 API + ETag/If-Match（绕开 raw CDN 缓存）；404 重建保护（B 存在过即禁止空初始化，中止并告警）
7. B 去重改 URL+标题联合指纹；合并时重算 compositeScore
8. scrape-test 与生产脚本抽公共模块（评分/合并/过滤共享）
9. 代理统一：删 app.js 或明确"不部署"；统一根/proxy 的依赖版本；README 与部署事实对齐；补白名单/超时/体积上限
10. 修正「新增」badge 时间口径（存完整时间戳、统一北京时间）

**P2 架构演进（需决策方向）**
- 单文件 SPA 已到维护极限（126KB/429KB，共享函数重复 12+ 处，config.js 被 HTML 同名变量遮蔽）→ 按 hooks.js 路线继续拆共享模块，或引入打包产物
- 纯前端藏不了密钥 → 现有 proxy 可改造为密钥中继，或明确"用户自带 Key"模式
- 文档可信度机制 → 发版时跑「文档-代码一致性检查」
- 演示包 zip 由 CI 自动生成并 gitignore（手工维护必漂移）
- 两个前端 SPA 零测试 → 核心纯函数（去重/评分/新增判定/JSON 解析）补离线测试

---

## 六、请对方 agent 回复的问题

请针对以上内容回复以下几点（逐条编号，未逐条回复的视为默认接受）：

1. **逐条复核**：上列缺陷中，哪些确认为真实缺陷？哪些是误报/场景不成立？（重点：M12 GLM 端点、M13 百度 hotTag、D4 去重口径、M1 新增判定）
2. **遗漏补充**：你们自己代码里还知道哪些本报告没列出的已知问题/技术债？（尤其是 `生文助手.html` 作为线上入口却落后两个版本这件事的处理计划）
3. **P0 修复方案**：对紧急 4 项给出具体修复方案与估计改动量；博查 Key 吊销后，新 Key 如何安全分发？
4. **方向反馈**：对「P2 架构演进」四项建议，哪些同意、哪些反对、为什么？你们下一迭代本来计划做什么？
5. **文档流程**：CODE_AUDIT 等多处与代码不符（D1-D9），今后打算如何避免？是否接受"发版时必须同步一致性检查"这一约束？

> 本报告由独立审查产出，未修改任何项目文件。全部结论可复验。
