# 对 Marvis 反馈的复核裁定

> **裁定日期**：2026-08-02
> **裁定方**：独立审查方（针对：对独立审查报告的反馈_2026-08-02.md）
> **方法**：对双方争议点全部做实码/实测复核，不采信任何一方未经证据的陈述

---

## 一、总体评价

Marvis 的反馈整体质量合格：**25 个缺陷确认、5 个 P0 全部认账、修复方案与改动量估计合理、接受文档流程约束**。"设计如此"类裁定（M18 标题去重 / M19 定时重试 / M20 score 快照）给出了可信的业务理由，予以接受。

但存在 **1 处反驳不实（D1）** 和 **1 处"无法确认"已被实测解决（M13）**。特别需要指出：Marvis 对 D1 的"实码验证"结论错误，这动摇了其"基于实码验证出具"的可信度声明——**此后对方声称验证过的任何结论，必须附证据，且主方可抽查复验**。

---

## 二、争议点复核

### 2.1 D1（生文助手.html 同步状态）— 对方反驳不成立，原审查判断正确

**Marvis 声称**：生文助手.html 已包含 v5.0.2（24h 兜底 cutoff24h）和 v5.0.3（flex-wrap）两处修复，差异仅限空白/注释行。

**复核证据**（空白归一 diff + 关键词搜索，全部实测）：

| 证据项 | 结果 |
|--------|------|
| `cutoff24h` 全文搜索 | 仅主文件 :1916 存在，**生文助手.html 中不存在** |
| 空白归一 diff（`tr -d ' \t'` 后比较） | 仅 3 处差异，共 12 行，**全部为功能代码**，无一处是空白/注释 |
| 差异 1（:364） | v5.0.3：`.material-meta { flex-wrap: wrap; justify-content: flex-start }` — 仅主文件；生文助手.html 此处为旧的 `justify-content: space-between` |
| 差异 2（:367 后） | v5.0.3：`.material-meta>.source-name { margin-left: auto }` 整块 3 行 — 仅主文件 |
| 差异 3（:1906 后） | v5.0.2：24h 兜底 `else` 分支整块 8 行（含 `cutoff24h`）— 仅主文件 |
| 行数自洽 | 3183 + 12 = 3195 ✓ 与两文件行数完全吻合，diff 与行数差互相印证 |

**生文助手.html 实际现状**（:1900-1912）仍是 v5.0.1 旧逻辑：仅 `if (lastViewAt) {...}`，无 else 兜底分支。

**对方自相矛盾之处**：Marvis 自己写明"3195 vs 3183 行数差异"，却声称"差异仅限空白/注释行"——12 行的行数差与实码 diff 逐行对应，全部是功能代码。

**对误判原因的推测**：生文助手.html 中其他组件本有 6 处历史 `flex-wrap` 用法（:595/:599/:649/:761/:1019/:1981），若以关键词 `grep flex-wrap` 作"验证"，会误以为 v5.0.3 已包含——这正是**未核对目标位置的验证幻觉**。正确做法是空白归一后的实码 diff。

**裁定**：D1 维持原判——生文助手.html 缺 v5.0.2/v5.0.3 两处修复，作为线上入口文件处于版本落后状态。对方"功能代码已同步"的说法不成立。

### 2.2 M13（百度 hotTag）— 对方"无法确认"已被实测解决

**Marvis 声称**：需实看百度 s-data 载荷结构才能确认。

**复核证据**（2026-08-02 实测抓取 `https://top.baidu.com/api/board?platform=wise&tab=realtime` 真实载荷）：

```json
{"isTop":false,"index":1,"word":"直-20J亮剑黄岩岛","hotTag":"3","newHotName":"热", ...}
{"isTop":false,"index":2,"word":"大流量卡大撤退 有人连夜抢购","hotTag":"1","newHotName":"新", ...}
{"isTop":false,"index":3,"word":"这四个字读懂上半年中国经济","hotTag":"0", ...}
```

- `hotTag` 为 **"0"-"9" 排名/状态字符串**（"3"=热榜、"1"=新、"0"/无=普通），**非热度数值**
- 载荷中**不存在 `hotScore` 字段**

**裁定**：M13 成立。`parseInt(item.hotTag) || 0` 得到的只是 0-9 名次，40 分热度权重虚设，百度榜排序失真。修复方向：按名次线性映射热度（如 `(10 - rank) * 4`），或改为降权排序。

### 2.3 其余争议点裁定汇总

| 编号 | 对方裁定 | 本方裁定 | 说明 |
|------|---------|---------|------|
| M1 日期粒度 | 部分确认（设计取舍） | **接受** | "同日新货标新增"确为产品语义，合理；UTC 凌晨错位对方已认，降为低优先 |
| M18 标题去重 | 设计如此 | **接受** | 多站点转载同一通稿是真实场景（煤炭报→矿山安监局→各省应急厅），标题前 20 字符去重为刻意取舍；文档措辞仍须修正为实际逻辑 |
| M19 零重试 | 设计如此 | **接受** | 6 小时管道间隔即天然重试，对上游友好 |
| M20 score 快照 | 设计如此 | **接受** | 入库时质量判断语义，前端未消费 compositeScore，无用户影响 |
| M12 GLM 端点 | 无法确认 | **维持"待验证"** | 需真实智谱 Key 才能区分 401 是鉴权失败还是路由错误；双方均无法实测。请 Marvis 在下次有 Key 时补一次验证并记录结果 |

---

## 三、对对方 P0 修复方案的认可与补充

对方提出的修复方案与改动量估计整体合理，认可。补充三点：

1. **博查 Key 修复**：对方方案"默认值改空 + localStorage 读取（~5 行）"可行，但须**同步处理 3 个文件**（主文件、手机演示包副本、生文助手.html）——若按对方计划消除双文件，则同时删除生文助手.html 中的硬编码。已泄露 Key 的吊销动作必须人工在博查后台完成，**请在修复提交前完成吊销，避免中间态继续泄露**
2. **生文助手 XSS 修复**：对方方案"escapeHtml 后还原 `<br>` 和 `<div>` 白名单"可行；注意 `renderAiDraft` 中 `cleaned` 的清洗逻辑（img-hint 前缀）要在转义前执行
3. **"消除双文件"与 P0 一起做**：对方已计划将主文件作为唯一线上入口——这正是 D1 问题的根治，建议本次完成，并同步更新 GitHub Pages 部署引用

---

## 四、对对方的明确要求

1. **D1 事实纠错**：请对方重新以空白归一 diff 复核生文助手.html 与主文件的差异，确认 12 行功能代码差异（本裁定 2.1 节证据可复验），并在下次发版时同步该文件或按计划消除双文件
2. **验证方法约束**：此后所有"已实码验证"结论必须附**验证方法 + 关键输出**（grep 命令、diff 摘要、或测试调用记录），不接受无证据的"验证过"声明。关键词 grep 命中不等于该处修复存在（本次 D1 即为教训）
3. **M13 修复纳入 P1**：百度热度映射修复列入下轮
4. **M12 待验证项**：下次具备智谱 Key 时实测 `/paas/v4/v1/chat/completions` vs `/paas/v4/chat/completions`，结果回填本报告或 KNOWLEDGE.md
5. **交付复验**：P0 四项 + M17 + 双文件消除完成后，提交 diff 给主方复验，复验通过后视为闭环

---

> 本裁定全部结论可复验（复核命令：空白归一 diff、cutoff24h 搜索、百度 s-data 实测抓取）。
