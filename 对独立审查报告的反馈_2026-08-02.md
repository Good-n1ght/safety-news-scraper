# 对独立审查报告的反馈

> **反馈日期**：2026-08-02
> **反馈方**：Marvis（项目主维护 Agent）
> **针对**：独立审查报告_2026-08-02.md

---

## 总体评价

审查报告质量很高，大部分缺陷真实存在，文档矛盾也抓得准。以下逐条复核，每条标注「确认」「部分确认」「有误」「设计如此」四档之一。

---

## 一、紧急问题（P0）复核

### 1.1 博查 API Key 泄露 — 确认

Key 确实硬编码在 HTML 中，公开仓库可查。接受整改建议，计划将 Key 移出源码，从设置面板 localStorage 读取。过往另一把 Key（`sk-64c2daf…`）的泄露已在 KNOWLEDGE 坑 15 记录。

### 1.2 gistTokenInput 未注册 — 确认（P0 级 bug）

实码验证：`initSettings` 中 els 映射确实缺少 `gistTokenInput`。HTML 有这个 input（1263 行），保存逻辑有防御性判空（1491 行），但 `els.gistTokenInput` 恒 undefined → 用户填的 GitHub Token 每次点保存都会被清空。一行修复。

### 1.3 强安视界正文 XSS — 确认

`renderGeneratedArticles` 中 `article.article` 直接拼入 innerHTML，未经转义。攻击链（第三方标题 → AI 提示词 → 模型输出 → DOM）合理。

### 1.4 生文助手 AI 草稿 XSS — 确认

`renderAiDraft`（2759 行）`els.articleBody.innerHTML = cleaned` 是全文唯一未转义渲染点，`cleaned` 只清理 img-hint 前缀，不经消毒。模型输出源自联网抓取正文，恶意网页可诱导 XSS payload。

### 1.5 开放代理 SSRF — 确认（但设计如此）

代理本身是开放 CORS 代理，设计上接受任意 URL。白名单会破坏灵活性。超时和响应体上限建议合理，应当加固。

---

## 二、中危缺陷复核

| 编号 | 缺陷简述 | 复核结果 | 说明 |
|------|---------|---------|------|
| M1 | 新增判定日期粒度粗、UTC 时区问题 | 部分确认 | `lastViewAt` 存 `YYYY-MM-DD` 而非完整时间戳，同日新货全天标新增。这是**设计取舍**：同日入库的货就该让用户看到新增标记，不是 bug。UTC 偏移在凌晨 0-8 点确实有错位，但实际影响极小（凌晨无人使用）。v5.0.2 的 24h 兜底已覆盖首访无 lastViewAt 缺口 |
| M2 | fetch 缺超时 | 确认 | 应封装 AbortController + 30s 超时 |
| M3 | saveDraft 无去重/竞态 | 确认 | 实际场景中多标签页同时保存极少发生，但仍应加固 |
| M4 | 缓存校验不完整 | 确认 | 缓存损坏确实会导致整页 JS 崩溃 |
| M5 | data-material-id 未转义 | 确认 | 攻击面很小（id 来自 Gist 内部数据），但防御原则应统一 |
| M6 | localStorage 无 try-catch | 确认 | 隐私模式/存储禁用会崩溃 |
| M7 | html2canvas 未判 typeof | 确认 | CDN 加载失败时静默失灵 |
| M8 | GIST_RAW 硬编码用户名 | 确认 | 当前为个人项目无多用户场景，但如果要开放需改 |
| M9 | 强安视界 16 个 fetch 无超时 | 确认 | 任一挂起导致 Promise.all 永不 settle |
| M10 | groundMode 复选框未注册 | 确认 | 功能静默失效 |
| M11 | fetchPexelsImages 从未调用 | 确认 | 死代码，配图功能未实现 |
| M12 | GLM 端点拼接 | 无法确认 | 未实测智谱 API 实际路由行为，需真实调用验证 |
| M13 | 百度 hotTag 是排名非热度 | 无法确认 | 需实看百度 s-data 载荷结构 |
| M14 | JSON 解析无 ```json 围栏处理 | 确认 | 模型输出带围栏即解析失败 |
| M15 | 强安视界顶层 localStorage 无 try-catch | 确认 | 存储禁用时页面完全空白 |
| M16 | 点击监听器叠加冲突 | 确认 | 点按钮意外翻转选中状态 |
| **M17** | Gist B 404 重建风险 | **确认（重点）** | CDN 抖动导致临时 404 → 触发旧文件迁移 → 历史 100 条被覆盖为 30 条。现有双保护挡不住「B 实际存在但读取被视为不存在」场景 |
| **M18** | B 去重按标题前 20 字符 | **确认（设计如此）** | 标题去重优于 URL 去重——同一新闻可能在不同源重复发布（不同 URL 但同一标题前缀）。文档应修正为实际逻辑，但代码本身没错 |
| M19 | 零重试 | 确认（设计如此） | 定时管道 6 小时自动重跑即天然重试，即时重试反而对上游造成压力 |
| M20 | compositeScore 从不重算 | 确认（设计如此） | 历史 score 是入库快照，代表入库时质量判断。重算无意义且会扭曲历史数据 |
| M21 | Gist B 读走 raw URL | 确认 | CDN 缓存可能导致读到陈旧数据 |
| M22 | Gist A 写入未 try-catch | 确认 | 部分成功状态下 A/B 无法对齐 |
| M23 | workflow 告警新建 Issue 无去重 | 确认 | 失败 4 次/天最多刷 4 条；缺少 label 时静默丢失 |
| **M24** | scrape-test 与生产 4 处不一致 | **确认（重点）** | 测试缺 timeBonus、阈值不同、合并逻辑不同、缺双保护。`--push` 模式会污染生产 Gist。应抽公共模块 |
| M25 | 代理多套入口不一致 | 确认 | 三套入口跑不同版本，依赖版本漂移 |
| M26 | 代理 URL 互不一致 + 文档矛盾 | 确认 | config.js 与 HTML 内硬编码的 3 个 SCF URL 互相不同；README 说废弃但还在跑 |

---

## 三、文档矛盾（D 类）复核

| 编号 | 审查判断 | 复核结果 | 说明 |
|------|---------|---------|------|
| **D1** | 生文助手.html 缺 v5.0.2/v5.0.3 | **有误** | 实码验证：生文助手.html **已包含 v5.0.2（24h 兜底 cutoff24h）和 v5.0.3（flex-wrap wrap）两处修复**。MD5 与主文件不同是因为少数行差异（3195 vs 3183），但功能代码已同步。审查方可能只比对了 MD5 未做实码 diff |
| D2 | proxy/app.js 仍在磁盘 | 确认 | 文档说已删除但实际未删 |
| D3 | 手机演示包已完全同步 | 已修正 | 审查方自述 7/30 已同步 |
| D4 | 去重口径/衰减公式文档不实 | 确认 | 文档应修正为实际逻辑：标题前 20 字符去重 + `1/(1+0.03×days)` 衰减 |
| D5 | 搜索"并行"描述不实 | 确认 | 实际串行 + 1.5s 间隔，文档措辞应修正 |
| D6 | 诊断页判定过时 | 确认 | test_phone_diag.html 在 v5.0.2 之前编写，未反映 24h 兜底逻辑 |
| D7 | CORS 代理"全线废弃"与部署矛盾 | 确认 | README 说废弃但部署配置仍在运行 |
| D8 | v5.0.2 日期不一致 | 确认 | CHANGELOG 写 07-31，git commit 实际为 08-01（跨午夜） |
| D9 | 审计文档内部矛盾 | 确认 | 多日增量审计未回填旧段落，应改为「增量条目 + 日期标注」模式 |

---

## 四、已确认正常的部分

审查方第四章所列已验证项，全部认可，无需复验。

---

## 五、回答审查方六个问题

### 问题 1：逐条复核结果

- **确认真实缺陷**：1.1~1.5（5 个 P0）+ M2~M11 + M14~M17 + M21~M26 = 共 25 个
- **部分确认**：M1（日期粒度是取舍，不是 bug）
- **设计如此，不算缺陷**：M18（标题去重）、M19（定时重试）、M20（score 是快照）
- **审查方误判**：D1（生文助手.html 功能代码已同步）
- **无法确认需实测**：M12（GLM 端点）、M13（百度 hotTag）

### 问题 2：遗漏补充

审查报告未提及的已知问题：

1. **双文件维护问题**：`生文助手.html` 是 GitHub Pages 线上入口，`强安兴企安全园地生文助手.html` 是主开发文件。目前手工拷贝同步，存在版本漂移风险。计划：将主文件直接作为唯一入口，消除双文件
2. **共享函数重复**：两个 HTML 中 `escapeHtml`、`normalizeAndCapMaterials`、`fetchTodayMaterials` 等函数重复 12+ 处，应抽公共模块
3. **手机演示包 zip 被 git 追踪**：241KB 二进制文件不应入仓库，应 CI 自动生成 + gitignore

### 问题 3：P0 修复方案与预估

| 缺陷 | 修复方案 | 改动量 |
|------|---------|--------|
| 博查 Key 泄露 | 代码中默认值改空，从 localStorage 读取（此机制已存在，只需去掉硬编码默认值）；已泄露 Key 去博查后台吊销换新 | ~5 行 |
| gistTokenInput | `els` 加一行 `gistTokenInput: document.getElementById("gistTokenInput")` | 1 行 |
| 强安视界 XSS | 正文先 `escapeHtml` 再替换 `\n` → `<br>` | ~5 行 |
| 生文助手 XSS | `cleaned` 先过 `escapeHtml` 再还原 `<br>` 和 `<div>` 标签（白名单方式） | ~10 行 |

新 Key 分发方式：去掉硬编码后，首次打开页面 API Key 为空，用户在设置面板填入。或通过 GitHub Pages Secrets 在 Actions 构建时注入。

### 问题 4：P2 架构演进方向反馈

| 建议 | 态度 | 理由 |
|------|------|------|
| 拆共享模块 | 同意 | 两个 HTML 共享函数重复 12+ 处是已知技术债。倾向 ES modules + importmap，不动构建工具以保持零依赖部署优势 |
| 密钥中继 | 同意方向，优先级低 | 改造 proxy 为中继可行，但增加运维复杂度。当前"用户自带 Key"模式更简单 |
| CI 自动生成 zip | 同意 | 手工维护必漂移，应自动化 + gitignore |
| 核心函数离线测试 | 同意 | 先覆盖 `normalizeAndCapMaterials`、去重、评分、新增判定四个纯函数 |

**无反对项**。

### 问题 5：文档流程改进

接受「发版时同步一致性检查」约束。具体措施：

- 每次代码改动后走固定 checklist：代码 → 六份文档 → 手机演示包 → 生文助手.html → zip → commit push
- CODE_AUDIT 改为「增量条目 + 日期标注」模式，旧段落不加"已修复"结论除非实码验证过
- 发版前跑文档-代码一致性 diff（关键函数签名、版本号、MD5）

### 问题 6：下一迭代计划

当前计划（按优先级）：
1. P0 四项止血（本次）
2. M1 时区修正 + M17 404 保护（本次）
3. 消除生文助手.html 双文件维护（本次）
4. scrape-test 与生产脚本抽公共模块（下轮）
5. 两个 HTML 共享函数拆公共模块（下轮）

---

## 六、补充说明：几处审查方可能不了解的背景

1. **M18 标题去重 vs URL 去重**：煤矿安全新闻来源集中在几个站点，同一篇通稿会被多家转载（如中国煤炭报 → 国家矿山安监局 → 各省应急厅），URL 完全不同但内容高度重复。标题前 20 字符去重是按业务场景选择的，不是疏忽。

2. **M19 零重试**：管道每日 4 次运行（07/13/19/01），单次失败 6 小时后自动重试。对于非实时场景，这比即时重试更合理——避免对上游 API 造成压力且不影响最终效果。

3. **v5.0.2 24h 兜底**：这个修复在审查报告中被提及（D6），但 M1 的判定分析似乎没考虑到它。`lastViewAt` 为空时会走 24h cutoff 分支，首访不会完全漏标。

4. **生文助手.html 同步状态**：D1 的判断基于 MD5 比对，但实码 diff 显示 v5.0.2 和 v5.0.3 的关键代码都已包含。差异仅限于少数空白/注释行。

---

> **反馈由 Marvis 基于实码验证出具，所有结论可复验。**
