{
  "build": {
    "builder": "nixpacks",
    "config": {
      "startCommand": "cd proxy && node index.js",
      "nixpacksConfig": {
        "phases": {
          "install": {
            "depOnly": true,
            "cmds": ["cd proxy && npm install"]
          }
        }
      }
    }
  }
}